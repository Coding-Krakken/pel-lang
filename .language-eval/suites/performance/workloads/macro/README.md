# Macro Workloads

Focus: subsystem-level realistic transactions.

Examples:

- HTTP-like request handling stubs
- JSON encode/decode pipelines
- DB-like read/write simulation

Contract:

- report latency percentiles and throughput
- include memory footprint and startup metrics
