id: CONF-PARSE-037
category: parsing
spec_ref: spec/pel_language_spec.md#expressions
description: Nested function calls
input: "model test {\n  var result = sqrt(abs(x));\n}\n"
expected:
  type: success
  ast:
    type: Model
    name: test
