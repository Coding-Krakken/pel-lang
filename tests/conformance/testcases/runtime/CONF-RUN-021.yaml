id: CONF-RUN-021
category: runtime
spec_ref: spec/pel_language_spec.md#section-15
description: Constraint checking test 1 - constraint checking
input: |
  model test {
    param x = 100;
    var y = x * 2;
  }
expected:
  values:
    y: 200
