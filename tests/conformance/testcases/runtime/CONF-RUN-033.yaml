id: CONF-RUN-033
category: runtime
spec_ref: spec/pel_language_spec.md#section-15
description: Constraint checking test 13 - constraint checking
input: |
  model test {
    param x = 100;
    var y = x * 2;
  }
expected:
  values:
    y: 200
